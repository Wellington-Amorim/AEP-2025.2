# Configurações do Banco de Dados (padrões locais)
db.url=jdbc:mysql://localhost:3306/doacoes?createDatabaseIfNotExist=true&serverTimezone=UTC
db.username=root
db.password=

# Pool de conexões (ajuste se necessário)
db.pool.maximumPoolSize=10
db.pool.minimumIdle=5
db.pool.idleTimeout=300000
db.pool.connectionTimeout=30000
db.pool.maxLifetime=600000

# Logging via Logback (UTF-8 já configurado em logback.xml)
